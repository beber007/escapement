# Line ending normalisation: LF in the repository for everything textual.
* text=auto

# Explicit binaries, never converted.
*.pdf binary
*.exe binary
*.xls binary
# Project files of the MSP430 configurator: a text header followed by binary data.
*.zot binary
